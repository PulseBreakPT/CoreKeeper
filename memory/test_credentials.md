# Test credentials
Not applicable: Nexus Word is an offline/localStorage game with no authentication or external API credentials.

Preview URL comes from APP_URL in /app/.env. Open `/` or `/palavras.html`.
No accounts were created or modified.